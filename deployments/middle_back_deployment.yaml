version: apps/v1
kind: deployment
metadata:
  app.kubernetes.io/name: lct_deployment
  app.kubernetes.io/part-of: lct_efs

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: fsm_validator

  template:
    metadata:
      labels:
        app.kubernetes.io/name: fsm_validator
    containers:
      # todo: finish
